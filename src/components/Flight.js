import React, { useState, useEffect } from "react";
import Flights from "./Flights";

function Flight({ flight }) {
  const [showFlight, setShowFlight] = useState(false);
  const [flyTo, flyFrom, id] = flight;

  return (
    
  );
}

export default Flight;
